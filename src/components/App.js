import React from "react";
import Counter from "./Counter/Counter.js";

const App = () => {
    return (
        <div>
            <Counter initialValue={0} />
        </div>
    );
};

export default App;